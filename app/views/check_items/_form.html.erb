<%= form_with(model: check_item, local: true) do |form| %>
  <% if check_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(check_item.errors.count, "error") %> prohibited this check_item from being saved:</h2>

      <ul>
      <% check_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :check_item_product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
